-- This migration addresses the schema drift related to the Lead.email field
-- The unique constraint was removed from the database but this change wasn't tracked in migrations

-- Empty migration to mark this state as intentional
-- No actual SQL commands needed since the database is already in the desired state
